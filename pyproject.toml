[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-fleet-os"
version = "0.1.0"
description = "TEMM — The Completion Runtime. Project-first AI production and completion system."
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.12"
dependencies = []
